# Review Feedback for PR #18 (post-merge)

> **审查者**：Claude
> **审查日期**：2026-06-13
> **审查依据**：spec `docs/tasks/W2-D2-25-product-kb-metadata-extract.md` v2.0 + Handoff `docs/handoffs/W2-D2-25-handoff.md`
> **审查时机**：**post-merge**（用户授权 NEEDS_HUMAN 自合并；本文档作为审查 trace 归档）
> **总评**：**RETROSPECTIVE_APPROVE**（NEEDS_HUMAN 合并合规，质量 9/10，1 个 follow-up）
> **last_reviewed_commit**：`587aa33`

---

## 1. 必须修复（Blocker）

**无**。

PR #18 总评 NEEDS_HUMAN 是流程性触发（D3/D4/D6 行数门槛 + 0003 lossy migration 警告），不是质量问题。本地 Part A/C 全过、CI self-review SUCCESS、168 passed/coverage 86%、PR #13 锚点 diff 空、3 条偏差均经用户明确放行、错误脱敏严格（错误消息 / review_reasons / logger 均不含 doc_path）。

---

## 2. 建议改进（Nice to have）

### N1. 🟡 删 `_route_by_extension()` 孤儿函数（follow-up 已立项）

- **位置**：`backend/app/services/product_kb_extract.py:79-84` + `backend/tests/services/test_product_kb_extract.py:64-72`
- **问题**：`_route_by_extension()` 是孤儿函数 —— 入口 `extract_product_kb_metadata` 没调它，且它对 `.pptx/.xlsx` 的判定（返回 `VISION`）跟入口逻辑**不一致**（入口对这些扩展名直接抛 `MetadataExtractError`）。两个测试 `test_vision_path_for_pptx` / `test_route_text_vs_vision_by_extension` 形成"假绿测试 + 调用方陷阱"。
- **风险**：#26 写批量入口时如果误用 `_route_by_extension` 会拿到错误的 VISION 结论，把 `.pptx/.xlsx` 喂给 pdf2image 触发底层异常。
- **建议**：删孤儿函数 + 删 2 个误导测试；入口真实行为已被 `test_unsupported_extension_raises_extract_error` / `test_pdf_with_extractable_text_routes_to_text` / `test_pdf_with_blank_text_routes_to_vision` / `test_text_path_extracts_all_fields` 完整覆盖。
- **优先级**：#26 启动前 micro-PR 修复。

### N2. 🟡 0003 lossy migration 未来生产数据风险（已在 Handoff 警告）

- **位置**：`backend/migrations/versions/0003_document_meta_acl_align.py:29` + `:65`
- **现状**：当前 PG `document_meta` 空表，零实际损失。
- **未来风险**：upgrade 把 sensitivity 全部折叠为 `3`；downgrade 把 owner_dept null → 空字符串、sensitivity → "normal"、`internal_only` → `internal`。
- **建议**：Handoff §5/§7 已明确"备份 + dry-run"，无需补丁；本条作为 review trace 记录，提醒 #28/#30 入库前再走一次环境检查。

---

## 3. 提问

**无**。Handoff §0-§8 完整、§3 偏差 3 条全部用户明确放行、§5 风险与 §7 暗坑都点到位（0003 lossy / Office 转换器未实现 / FieldConfidence.value 未强制 enum）。

---

## 4. 表扬（鼓励性反馈）

- 👍 Task 0 严格不动 PR #13 锚点 `app/models/document_meta.py`（`git diff main` 空），主动发现下游 `DocumentMetaCreate` 契约缺口并征得放行，**审查纪律满分**。
- 👍 错误脱敏三道一致：`MetadataExtractError("Failed to extract metadata")` 默认对外消息 + `review_reasons` 只含扩展名 + `logger.warning` 只记扩展名和异常类名。`test_extract_error_does_not_leak_doc_path` 直接断言 `secret_path not in str(exc)`，**安全契约写进测试名**。
- 👍 `test_pdf_with_extractable_text_routes_to_text` / `test_pdf_with_blank_text_routes_to_vision` 把"PDF 路由不引入字符阈值/图像密度分析"的规则锁进测试名，未来任何想加阈值的 PR 都会被这两个 case 拦下来。
- 👍 ACL 完整映射用 `@pytest.mark.parametrize` 一次覆盖 7 个 DocCategory，包含 release_note/other 兜底 case。`shared_depts == []` 在每条 case 都 assert，杜绝默认值漂移。
- 👍 pypdf/pdf2image 阻塞调用通过 `anyio.to_thread.run_sync` 卸载（line 51, 57），async 主线程不被 IO 卡住，并发友好。
- 👍 Handoff §B8 "下一轮暗坑" 3 条全部具体到行号 + 工程原因（0003 类型转换有损 / VISION 分类 ≠ 已实现转换器 / FieldConfidence 未强制 enum），给 #26 留了清晰的入口提示。
- 👍 fix_attempt 0 次，TDD 先红再绿一次过；CI 首次 run `27431635538` 仅因 Handoff 文件未创建而 A7 失败，推送后立刻绿，**展现成熟的 TDD 工作流**。

---

## 5. 总评说明

**RETROSPECTIVE_APPROVE**：NEEDS_HUMAN 自合并合规，质量 9/10，1 个 follow-up（N1）。

PR #18 把 1547 行新增 / 17 文件改动控制在"无 Blocker + 无静默偏差"水准，体现了 Codex 在 P4 不派 sub-agent 大任务上的工程能力：

- Task 0 + 主体强耦合的契约修复 + 抽取逻辑由主 agent 集中维护，没有并行冲突
- 三次中途歧义（DocumentMetaCreate 契约缺口 / PDF 路由规则 / Office 转换策略）全部主动暴露给用户决策，没有自作主张
- 错误脱敏 / Pydantic 锚点纪律 / lean MVP 边界（不入库、不并发、不接 CRM/IdP）全部到位

唯一扣 1 分的地方是 N1 孤儿函数 —— spec §4.3 设计稿里有 `_route()` 这个 helper，Codex 实现时把它的逻辑 inline 到了入口（更好的设计），但保留了 spec 草案的 `_route_by_extension()` 函数和测试，形成假绿测试。这是"实现演化 vs spec 草稿"对齐时的常见疏漏，由 #26 启动前 micro-PR 修复。

---

## 6. 沉淀任务

### F1. 删 `_route_by_extension()` 孤儿函数 + 误导测试

- **触发时机**：#26 spec 启动前
- **范围**：
  - 删 `backend/app/services/product_kb_extract.py:79-84`（6 行）
  - 删 `backend/tests/services/test_product_kb_extract.py:64-72`（2 个测试 = 9 行）
- **验证**：跑 `uv run pytest tests/services/test_product_kb_extract.py` 应 `18 passed`（原 20 - 删 2）；非 integration 全量应 `151 passed`（原 153 - 删 2）。
- **PR 命名**：`refactor: remove orphan _route_by_extension helper (#25 follow-up)`

### F2. 0003 生产数据迁移前的环境检查

- **触发时机**：#28 双轨入库 spec 实施前
- **范围**：在 #28 Handoff §6/§7 重复一次 "0003 lossy migration 警告 + 备份 + dry-run" 提醒
- **验证**：#28 spec 中加入 "执行 alembic upgrade 前 PG 备份" 步骤检查项

---

@用户：本 review 仅作为审查 trace 归档，无需任何动作。F1 由我在你合并 PR #18 后立刻执行（新分支 micro-PR）；F2 写入 #28 spec 时自动触发。
