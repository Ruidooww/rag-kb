# Review Feedback for PR #20 (post-merge)

> **审查者**：Claude
> **审查日期**：2026-06-13
> **审查依据**：spec `docs/tasks/W2-D2-26-batch-metadata-extract.md` v2.0 + Handoff `docs/handoffs/W2-D2-26-handoff.md`
> **审查时机**：**post-merge**（NEEDS_HUMAN 经用户授权合并；本文档作为审查 trace 归档）
> **总评**：**RETROSPECTIVE_APPROVE**（代码层 PASS / 跑批 deferred / tqdm license 落白名单 / 1 个 follow-up）
> **last_reviewed_commit**：`040f35b`

---

## 1. 必须修复（Blocker）

**无**。

PR #20 总评 NEEDS_HUMAN 是两个流程性触发：

1. **D5 tqdm license**（`MPL-2.0 AND MIT`）—— 见 §2 N1，**实际落白名单内**
2. **Spec §5.2 跑批 deferred** —— 用户凌晨明确放行的外部环境阻塞（`.env` 占位符 key），不是代码问题

代码层 100% PASS：14 项目标测试 / 165 non-integration / coverage 86% / 入口物理锁定 grep 无命中 / 失败二分 grep 无命中 / 范围 grep 无命中 / 脱敏 grep 无命中 / 静态检查全过。

---

## 2. 建议改进（Nice to have）

### N1. ✅ tqdm license 评估：放行（已在合并决策中确认）

- **位置**：`backend/pyproject.toml` 新增 `tqdm>=4.66`
- **PyPI metadata**：`MPL-2.0 AND MIT`
- **真相**：tqdm 项目实际是 dual-licensed（用户可选 MPL OR MIT），PyPA SPDX `AND` 表达式是歧义写法。文件主体 MIT，一小部分 MPL 2.0。
- **本项目使用方式**：只引用、不修改 tqdm 源码 → MPL 2.0 的 weak copyleft 不触发 → 等同按 MIT 使用 → 在白名单内
- **结论**：合规通过，无 license 阻碍

### N2. 🟡 env sanity check follow-up（**已立项**，本 PR 同分支顺手做）

- **背景**：Codex E1.3 自审反思指出"试水前只检查 `LLM_API_KEY` 是否非空，没检查是否为占位符/非 ASCII"。本次 5 份诊断试水耗时 30 秒才暴露根因。
- **F1（本 PR 同分支）**：在 `backend/scripts/_env_check.py` 加 `assert_llm_api_key_is_real()`：ASCII + `sk-` 前缀 + 长度 ≥ 20；`batch_extract_product_kb.py` 启动时调用。
- **不进 #25 / #26 入口**：保持 `extract_product_kb_metadata` 入口纯净（铁律 #1 + #2），sanity check 是 CLI 启动期边界检查，不是业务代码。

### N3. 🟡 0003 lossy migration 提醒（已在 PR-18 N2 跟踪）

PR #20 范围未涉及 PG 写入；`.gitignore` 已加 `backend/data/metadata.xlsx` 等产出。下游 #28 入库前仍需走 PR-18 F2 的 0003 dry-run 流程。

---

## 3. 提问

**无**。Handoff §0-§8 完整、§3 偏差 3 条全部用户明确放行（跑批 deferred / 取消预算阈值 / `mypy scripts` baseline 修复均有清晰理由）、§5 已知问题 + §7 下一轮提示均到位。

---

## 4. 表扬（鼓励性反馈）

- 👍 `batch_extract_product_kb.py` 入口物理锁定 100% 执行：grep 无任何 `_route_*` / `_extract_pdf_text` / `_to_pngs` / `_call_llm_*` 私有 helper 调用。**孤儿函数陷阱不会重演**。
- 👍 失败二分测试名锁规则：`test_metadata_extract_error_writes_needs_review_row` + `test_unexpected_exception_appends_failed_jsonl_without_path_internals` —— 把 spec §3.4/§3.5 设计契约硬编码到测试名，未来想合并两条路径的 PR 会被这两个 case 拦下。
- 👍 JSONL 脱敏严格：每行只有 `{doc_path, ext, error_type}` 3 个 key，**无 traceback / 无异常 message / 无路径外内部细节**。`test_unexpected_exception_appends_failed_jsonl_without_path_internals` 用 keys 集合 assert 断言。
- 👍 共享 schema 单源真相：`_xlsx_schema.py::METADATA_COLUMNS` 24 列锁住，`test_xlsx_schema.py` 3 项快照断言（含字段名跟 `ProductKBMetadata` / `DocumentMetaSchema` 锚点同步）。**#27 / #28 直接 import 即可，杜绝下游漂移**。
- 👍 5 份诊断试水**主动暴露问题、立即停下、不浪费**：30 秒发现 key 占位符问题，0 费用消耗（UnicodeEncodeError 在请求构造时本地抛出），全部进 needs_review xlsx（验证了失败二分逻辑），不写 jsonl（验证了"预期 vs 意外"边界）。**这是最理想的 fail-fast 演示**。
- 👍 主动修 `mypy scripts` baseline（启用 `explicit_package_bases` + 修 `verify_langgraph.py` 返回类型），让硬指标可执行。**遇到激活的依赖问题不绕过，而是修通**。
- 👍 fix_attempt 0：TDD 一次过；CI 首次 run PASS。
- 👍 Handoff §B8 "下一轮暗坑" 3 条全部具体到行号 + 工程原因（断点续跑路径键匹配 / 占位 key 空行影响 resume / 中途终止丢失未写盘结果），给 #27 / #28 留了清晰入口提示。
- 👍 自审 E1.3 主动反思"key sanity check 应在运行验收前加"—— 这种**没人提就主动暴露未来风险**的自审深度，是高质量 PR 的标志。

---

## 5. 总评说明

**RETROSPECTIVE_APPROVE**：代码层 PASS / 跑批 deferred / tqdm license 落白名单 / 1 个 follow-up（F1 env sanity check，本 PR 同分支顺手做）。

PR #20 质量打 10/10。NEEDS_HUMAN 两个触发都是**好的 NEEDS_HUMAN**：

- D5 tqdm license：Codex 没擅自把"我看着像 MIT"当结论，而是把决策推给 reviewer —— **合规审慎**
- 跑批 deferred：5 份试水主动暴露了配置问题、立即停下、不烧 token —— **fail-fast 教科书**

PR #20 是"#25 单文档抽取入口"到"#27 审核 + #28 入库"中间的关键契约层。Codex 把 24 列共享 schema、失败二分语义、入口锁定、JSONL 脱敏全部用测试名锁定，等于把"如何使用 #26 产出"这一段写进了 git 历史的契约里。**对下游 #27 / #28 / #30 而言，这是最有价值的工程资产之一**。

---

## 6. 沉淀任务

### F1. 加 `LLM_API_KEY` sanity check CLI 边界检查

- **触发时机**：本 PR 同分支顺手做（avoid 单独 micro-PR 开销）
- **范围**：
  - 新建 `backend/scripts/_env_check.py`：`assert_llm_api_key_is_real()` 函数（ASCII + `sk-` 前缀 + 长度 ≥ 20，失败抛 `ConfigError`）
  - 改 `backend/scripts/batch_extract_product_kb.py` 入口最前调用 sanity check
  - 加 `backend/tests/scripts/test_env_check.py` ≥ 4 项（valid / non-ASCII / no-sk-prefix / too-short）
- **不进**：
  - `app/services/llm.py`（铁律 #1 + #2，LLM 抽象层不该硬编码 key 格式假设）
  - `app/core/config.py` pydantic validator（单元测试用 mock key 会被拦截）
- **PR 名**：`chore: #26 follow-up — add env sanity check + PR #20 review`

### F2. key 就位后补跑 + 回填

- **触发时机**：你拿到真实百炼 key 后
- **顺序**：
  1. 改 `.env` `LLM_API_KEY=sk-xxxxxxxx`（VS Code UTF-8 no BOM）
  2. `uv run python scripts/batch_extract_product_kb.py ./data/sample_5 ./data/metadata_sample.xlsx`
  3. 人工核对 6 字段 + 路由分布写到 Handoff §4 表格
  4. `uv run python scripts/batch_extract_product_kb.py "C:\Users\Ruidoww\Documents\IPG知识库构建" ./data/metadata.xlsx`
  5. 全量数据回填 Handoff §4 + 标 done
- **不开新 PR**：直接 commit 到 main（docs-only 改 Handoff §4 deferred → 真实数据）

---

@用户：本 review 仅作为审查 trace 归档，无需任何动作。F1 由我立刻执行（同分支顺手做）；F2 在你拿到真实 key 后任何时间手动跑。
