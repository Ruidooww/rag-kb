# Review Feedback for PR #16 (post-merge)

> **审查者**：Claude
> **审查日期**：2026-06-05
> **审查依据**：spec `docs/tasks/W2-D4-71-tool-registry-assertion-test.md` + Handoff `docs/handoffs/W2-D4-71-handoff.md`
> **审查时机**：**post-merge**（用户授权 Codex 自合并 PASS 级别 PR；本文档作为审查 trace 归档）
> **总评**：**RETROSPECTIVE_APPROVE**（PASS 级别自合并合规，无 follow-up）
> **last_reviewed_commit**：`be8d423`

---

## 1. 必须修复（Blocker）

**无**。

PR #16 总评 PASS、240 行 / 4 文件、Codex 自审 §3 无偏差、RED 验证（删 snapshot → fail）显式跑过、tool_registry.py 覆盖率 100%、全量 102 测试通过。**典型的"高质量小任务"模板。**

---

## 2. 建议改进（Nice to have）

### N1. 🟡 跟 PR #15 N3 联动：建议改白名单反向断言

- **位置**：`backend/tests/agents/test_tool_registry.py` + `backend/tests/agents/tool_registry_snapshot.json`
- **当前**：snapshot 用静态 JSON 列举 EXTERNAL / INTERNAL 工具名，配合 `tool_registry.py` 顶层黑名单前缀 assert。
- **建议**：见 PR-15.md §2 N3。如果 `tool_registry.py` 改成白名单反向断言（`_ALLOWED_EXTERNAL_TOOL_NAMES`），本测试可同步引用同一个白名单，避免双源真相。
- **优先级**：跟 PR #15 N3 同 commit 修，不单独做。

---

## 3. 提问

**无**。Codex Handoff §5 风险已主动列「快照变更需要 reviewer 看清楚原因，测试只能防止悄悄改」+「`allowed_external` 白名单模式未引入」，自我评估足够诚实。

---

## 4. 表扬（鼓励性反馈）

- 👍 `tests/agents/test_tool_registry.py:30` snapshot 缺失时显式 `pytest.fail` 含修复指引，比静默 skip 强一个数量级。
- 👍 `tests/agents/tool_registry_snapshot.json` 加了 `_comment` 字段说明"任何变更必须同步更新 + PR 描述说明原因"，让 diff 时 reviewer 第一眼看到契约。
- 👍 RED 验证主动跑过（Handoff §4 表第 1 行 + §B5），证明断言确实生效，不是"GREEN 直接过"的伪覆盖。
- 👍 `backend/pyproject.toml:62` 注册 `tool_registry` pytest marker，消除 pytest 的 unknown marker warning，CI 输出干净。
- 👍 4 个 spec §4 项全部完成，Handoff §3 无偏差，§5 风险与 §7 暗坑都点到位（snapshot ≠ 安全保证；新增外部工具必须配 prompt injection 测试）。
- 👍 测试运行 < 1 秒（Handoff §4 表 `0.44s`），满足 spec §3 验收目标 5 项之一。
- 👍 100% 覆盖 `tool_registry.py`（8 statements / 0 missed），证明断言路径全覆盖。
- 👍 Handoff §7 上下文 3「#72 可以读取 snapshot 生成 prompt injection fixture 基线」— 主动给下一轮留了具体 API hook，前瞻性好。

---

## 5. 总评说明

**RETROSPECTIVE_APPROVE**：PASS 级别自合并合规，质量 10/10，无 follow-up。

PR #16 是"高质量小任务"的典型：spec 清晰 + 范围严格控制 + RED/GREEN 验证完整 + Handoff 诚实简洁。这种任务 Codex 走 L1 标准流程效率很高，是未来类似落地（如 #71 类的「断言/快照/合规检查」任务）的参考模板。

---

## 6. 沉淀任务

**无**。本 PR 无独立 follow-up；N1 跟 PR #15 N3 联动批处理。

---

@用户：本 review 仅作为审查 trace 归档，无需任何动作。
